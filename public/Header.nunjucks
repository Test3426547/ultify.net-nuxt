<!-- components/Header.nunjucks -->
<div class="header">
  <h1>{{ title }}</h1>
  <p>{{ subtitle }}</p>
  <h2>{{ heading }}</h2>
  <p>{{ subheading }}</p>
  <ul>
    {% for link in links %}
      <li><a href="{{ link.path }}">{{ link.name }}</a></li>
    {% endfor %}
  </ul>
</div>